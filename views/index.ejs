<% include ./partials/header %>

    <div class="ui piled segment">
        <h1 id="main-header">Mongo Scrapper</h1>

        <p id="main-footer">New Yok Times Edition</p>
    </div>

    <hr>

    <% results.forEach((result) => { %>
        <div class="ui yellow segment">
            <h2>
                <%= result.link %>
            </h2>
            <hr>
            <p>
                <%= result.body %>
            </p>
        </div>
        <% }) %>



            <% include ./partials/footer %>